---
phase: 06-evil-doom-integration
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: [emacs/sss-mode.el]
autonomous: true
requirements: [DOOM-01, DOOM-02]

must_haves:
  truths:
    - "`SPC e` in Doom Emacs opens an encryption prefix with encrypt/decrypt/toggle/preview/project/keys commands"
    - "`, e` in sss-mode buffers (localleader) provides buffer-local encrypt/decrypt/toggle/preview/menu commands"
    - "Loading sss-mode.el outside Doom (no map! function) produces no errors — Doom code is gated behind (when (fboundp 'map!) ...)"
  artifacts:
    - path: "emacs/sss-mode.el"
      provides: "Doom leader bindings under SPC e, localleader bindings under , e in sss-mode-map, all inside (when (fboundp 'map!) ...) guard"
      contains: "when (fboundp 'map!)"
  key_links:
    - from: "map! :leader (:prefix-map (\"e\" . \"encryption\") ...)"
      to: "sss-encrypt-region, sss-decrypt-region, sss-toggle-at-point, sss-preview-at-point, sss-dispatch, sss-init, sss-process, sss-keygen, sss-keys-list"
      via: "Doom leader prefix SPC e"
      pattern: ":leader.*:prefix-map.*encryption"
    - from: "map! :localleader :map sss-mode-map"
      to: "sss-encrypt-region, sss-decrypt-region, sss-toggle-at-point, sss-preview-at-point, sss-dispatch"
      via: "Doom localleader prefix , e in sss-mode buffers"
      pattern: ":localleader.*sss-mode-map"
---

<objective>
Add Doom Emacs leader and localleader bindings to `emacs/sss-mode.el`, gated behind `(when (fboundp 'map!) ...)`.

Purpose: Doom users get idiomatic `SPC e` prefix for project/key/encryption commands and `, e` localleader prefix for buffer-local operations in sss-mode buffers — with graceful degradation when Doom is absent.

Output: Updated `emacs/sss-mode.el` with Doom integration block appended after the evil block, before `(provide 'sss-mode)`.
</objective>

<execution_context>
@/home/dsp/.claude/get-shit-done/workflows/execute-plan.md
@/home/dsp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-evil-doom-integration/06-RESEARCH.md
@.planning/phases/06-evil-doom-integration/06-01-SUMMARY.md
@emacs/sss-mode.el
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Doom leader and localleader bindings</name>
  <files>emacs/sss-mode.el</files>
  <action>
Insert a new section AFTER the `(with-eval-after-load 'evil ...)` block (from Plan 06-01) and BEFORE `(provide 'sss-mode)`. Add a section comment: `;;; Doom integration (DOOM-01, DOOM-02)`

Wrap the entire Doom block in `(when (fboundp 'map!) ...)`. This guard ensures map! is defined before calling it — in vanilla Emacs, `map!` is void and this block is skipped entirely.

**Global leader bindings (DOOM-01):** `SPC e` prefix with nested sub-prefixes for project and keys:

```elisp
(map! :leader
      (:prefix-map ("e" . "encryption")
       :desc "Encrypt region"   "e" #'sss-encrypt-region
       :desc "Decrypt region"   "d" #'sss-decrypt-region
       :desc "Toggle at point"  "t" #'sss-toggle-at-point
       :desc "Preview at point" "v" #'sss-preview-at-point
       :desc "SSS menu"         "SPC" #'sss-dispatch
       (:prefix ("p" . "project")
        :desc "Init project"    "i" #'sss-init
        :desc "Process project" "p" #'sss-process)
       (:prefix ("k" . "keys")
        :desc "Generate keys"   "g" #'sss-keygen
        :desc "List keys"       "l" #'sss-keys-list)))
```

Key layout rationale:
- `SPC e e` — encrypt (mnemonic: "e" for encrypt)
- `SPC e d` — decrypt
- `SPC e t` — toggle
- `SPC e v` — preview (view)
- `SPC e SPC` — dispatch menu
- `SPC e p i` — project init
- `SPC e p p` — project process
- `SPC e k g` — keys generate
- `SPC e k l` — keys list

**Localleader bindings (DOOM-02):** `, e` prefix in sss-mode buffers only:

```elisp
(map! :localleader
      :map sss-mode-map
      (:prefix ("e" . "sss")
       :desc "Encrypt region"   "e" #'sss-encrypt-region
       :desc "Decrypt region"   "d" #'sss-decrypt-region
       :desc "Toggle at point"  "t" #'sss-toggle-at-point
       :desc "Preview at point" "v" #'sss-preview-at-point
       :desc "SSS menu"         "SPC" #'sss-dispatch))
```

The localleader is `, ` per user's config (`doom-localleader-key` = ","). The `:map sss-mode-map` ensures these only activate in sss-mode buffers.

IMPORTANT:
- Do NOT call `(require 'sss)` or `(require 'sss-ui)` — these are old plugin packages being deleted in Phase 7. All target functions are already defined earlier in sss-mode.el.
- Do NOT use `(with-eval-after-load 'doom-core ...)` — `(when (fboundp 'map!) ...)` is simpler and achieves the same result.
- Do NOT use `(featurep 'evil)` inside the Doom block for operator bindings — the operators are already bound to ge/gd/gt in sss-mode-map by Plan 06-01's evil-define-key. Doom's map! for operators is unnecessary and would create global bindings we explicitly avoided.
- All `:desc` strings provide which-key descriptions for Doom's discoverable key hints.
  </action>
  <verify>
Run: `emacs -Q --batch --eval '(byte-compile-file "emacs/sss-mode.el")'`

Must produce:
- Exit 0
- No errors
- No "free variable" warnings for map! (it's inside an fboundp guard; byte-compiler may warn but the guard prevents execution)

Also verify clean load without Doom:
`emacs -Q --batch --load emacs/sss-mode.el --eval '(message "loaded OK")'`

Must print "loaded OK" with no errors about map!, doom-core, or undefined functions.

Grep checks:
- `grep -c 'fboundp.*map!' emacs/sss-mode.el` — exactly 1 occurrence
- `grep -c ':leader' emacs/sss-mode.el` — exactly 1 occurrence
- `grep -c ':localleader' emacs/sss-mode.el` — exactly 1 occurrence
- `grep -c 'require.*sss' emacs/sss-mode.el` — 0 occurrences of (require 'sss) (there should be 0; auth-source require is different)
  </verify>
  <done>
Doom leader bindings (`SPC e` prefix) with 9 commands across 3 groups (operations, project, keys). Doom localleader bindings (`, e` prefix) with 5 buffer-local commands in sss-mode-map. All inside `(when (fboundp 'map!) ...)` guard. File byte-compiles and loads cleanly without Doom or evil.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify complete file structure and update version commentary</name>
  <files>emacs/sss-mode.el</files>
  <action>
After adding the Doom block, verify the file has this structure at the bottom (after the mode definition and interactive wrappers):

```
;;; Evil integration (EVIL-01, EVIL-02, EVIL-03, DOOM-03)
(with-eval-after-load 'evil
  ...operators...
  ...key bindings...
  ...text objects...
  ...text object registrations...)

;;; Doom integration (DOOM-01, DOOM-02)
(when (fboundp 'map!)
  ...leader bindings...
  ...localleader bindings...)

(provide 'sss-mode)
;;; sss-mode.el ends here
```

Update the Commentary section at the top of the file to document the new Phase 6 features. Add after the existing "New in v1.1:" block:

```
;; Evil integration (when evil-mode is loaded):
;; - ge/gd/gt operators for encrypt/decrypt/toggle via motions (sss-mode buffers only)
;; - is/as text objects for selecting SSS marker content/boundaries
;;
;; Doom Emacs integration (when Doom is detected):
;; - SPC e prefix for encryption commands (leader)
;; - , e prefix for buffer-local sss operations (localleader)
```

Verify the entire file byte-compiles cleanly and loads in vanilla Emacs without errors. This is the final validation that DOOM-03 (conditional loading) works end-to-end.
  </action>
  <verify>
1. `emacs -Q --batch --eval '(byte-compile-file "emacs/sss-mode.el")'` — exit 0, no errors
2. `emacs -Q --batch --load emacs/sss-mode.el --eval '(message "loaded OK")'` — "loaded OK", no errors
3. Verify structure: `grep -n 'with-eval-after-load\|fboundp.*map!\|provide.*sss-mode\|sss-mode.el ends here' emacs/sss-mode.el` — should show evil block, doom guard, provide, and footer in correct order
4. Verify commentary updated: `grep 'Evil integration\|Doom Emacs integration' emacs/sss-mode.el` — both present in commentary
5. Count total lines: `wc -l emacs/sss-mode.el` — should be approximately 710-730 lines (was ~637, adding ~70-90 lines for evil+doom blocks)
  </verify>
  <done>
Commentary updated to document evil and Doom features. File structure verified: evil block, doom block, provide, footer — in correct order. File byte-compiles cleanly. File loads in vanilla Emacs without errors. All 6 requirements (EVIL-01, EVIL-02, EVIL-03, DOOM-01, DOOM-02, DOOM-03) addressed across Plans 01 and 02.
  </done>
</task>

</tasks>

<verification>
1. `emacs -Q --batch --eval '(byte-compile-file "emacs/sss-mode.el")'` — exit 0, clean compile
2. `emacs -Q --batch --load emacs/sss-mode.el --eval '(message "loaded OK")'` — vanilla Emacs load test (DOOM-03)
3. File structure: evil block -> doom block -> provide -> footer (correct order)
4. `grep -c 'fboundp.*map!' emacs/sss-mode.el` — exactly 1 (DOOM-03 guard)
5. `grep -c ':leader' emacs/sss-mode.el` — exactly 1 (DOOM-01)
6. `grep -c ':localleader' emacs/sss-mode.el` — exactly 1 (DOOM-02)
7. `grep -c 'modulep!' emacs/sss-mode.el` — exactly 0 (never use Doom-internal macro)
8. `grep -c "require 'sss[^-]" emacs/sss-mode.el` — exactly 0 (no old plugin requires)
</verification>

<success_criteria>
- Doom leader prefix `SPC e` with 9 bindings across operations/project/keys groups (DOOM-01)
- Doom localleader prefix `, e` with 5 buffer-local bindings in sss-mode-map (DOOM-02)
- All Doom code inside `(when (fboundp 'map!) ...)` guard
- Commentary updated to document evil and Doom features
- File byte-compiles and loads cleanly in vanilla Emacs
- Combined with Plan 06-01: all 6 Phase 6 requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/06-evil-doom-integration/06-02-SUMMARY.md`
</output>
