# Rust build artifacts
/target/
/build
*.rpm
# Phase 26 / BUILD-01 (D-V23-01): Cargo.lock IS committed for reproducible
# builds. Bumps happen via deliberate `cargo update -p <crate>` + commit.
# See docs/SUPPLY-CHAIN.md and docs/RELEASE.md § Reproducible Build.

# IDE files
.vscode/
!vscode/.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Environment files
.env
.env.local

# Backup files
*.bak
*.backup

# SSS specific
.sss.toml
# Runtime rotation backups created by `sss keys rotate`
.sss_backup_*/

# Emacs bytecode
*.elc

# Temporary build/test documentation
BUILD_AND_USE.md
COVERAGE_REPORT.md
REPORT.md
sss-old/
sss-master/

# Packaging and build infrastructure
/debian/
/dist/
# Release staging directories (e.g. release-2.0.1/) — generated locally, not committed
release-*/
# Phase 25 SIGN-01 exception: docs/release-keys/ holds committed PUBLIC keys
# for release-artefact signing verification (private keys never live here).
!docs/release-keys/

# GSD workflow metadata (local planning artefacts, not for distribution)
/.planning/

# Local build-host vendored headers (FUSE, etc.) — not for distribution
/usr/

# Container build artifacts
*.tar.gz.asc

# Phase 10 / DEPS-01 — project-local cargo install root for cargo-audit/deny/machete
# (sandbox keeps ~/.cargo/bin read-only; tools live here for the audit run)
/.gsd-tools/

# Claude Flow generated files
.claude/settings.local.json
.mcp.json
claude-flow.config.json
.swarm/
.hive-mind/
.claude-flow/
memory/
coordination/
memory/claude-flow-data.json
memory/sessions/*
!memory/sessions/README.md
memory/agents/*
!memory/agents/README.md
coordination/memory_bank/*
coordination/subtasks/*
coordination/orchestration/*
*.db
*.db-journal
*.db-wal
*.sqlite
*.sqlite-journal
*.sqlite-wal
claude-flow
# Removed Windows wrapper files per user request
hive-mind-prompt-*.txt

# macFUSE build artifacts (abandoned macOS FUSE support)
Library/
cross/
osxcross/
*macfuse*
*MACFUSE*
build-macos*.sh
install-macos.sh
docs/*FUSE*.md
docs/*MACOS*.md
*MACOS_BUILD*.md
BUILD_FIX_SUMMARY.md
sss-mac
