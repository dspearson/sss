---
phase: 04-documentation
plan: 04
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
  - 04-03
files_modified:
  - README.md
autonomous: true
requirements:
  - DOC-01

must_haves:
  truths:
    - "README contains a working quickstart that starts with key generation, not just sealing"
    - "README installation section covers cargo install, build from source, and pre-built packages (RPM/DEB)"
    - "README links to all docs/ files created in Plans 01-03"
    - "README does not claim age crate or scrypt — correctly states libsodium XChaCha20-Poly1305 and Argon2id"
    - "A new user can follow the README quickstart from install through first sealed file without consulting source code"
  artifacts:
    - path: "README.md"
      provides: "Overhauled project README with quickstart, installation, feature overview, and doc links"
  key_links:
    - from: "README.md"
      to: "docs/usage-guide.md"
      via: "Link from quickstart/usage section"
      pattern: "docs/usage-guide\\.md"
    - from: "README.md"
      to: "docs/security-model.md"
      via: "Link from security section"
      pattern: "docs/security-model\\.md"
    - from: "README.md"
      to: "docs/architecture.md"
      via: "Link from architecture section"
      pattern: "docs/architecture\\.md"
    - from: "README.md"
      to: "docs/marker-format.md"
      via: "Link from markers section"
      pattern: "docs/marker-format\\.md"
    - from: "README.md"
      to: "docs/configuration.md"
      via: "Link from configuration section"
      pattern: "docs/configuration\\.md"
    - from: "README.md"
      to: "docs/sss-mode-guide.md"
      via: "Link from Emacs section"
      pattern: "docs/sss-mode-guide\\.md"
---

<objective>
Overhaul README.md (DOC-01) to provide a clear, beginner-friendly entry point with working quickstart, accurate installation instructions, and links to all documentation created in Plans 01-03.

Purpose: The README is the first thing a new user sees. It must provide a clear path from "what is this?" to "I have it working" without consulting source code. The current README is dense and flat with inaccuracies (age crate claim, missing key generation in quickstart). This plan replaces it with a structured, accurate README that links to the detailed docs/ files.

Output: README.md (overhauled)
</objective>

<execution_context>
@/home/dsp/.claude/get-shit-done/workflows/execute-plan.md
@/home/dsp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-documentation/04-RESEARCH.md

Prior plan SUMMARYs (needed for cross-link verification):
@.planning/phases/04-documentation/04-01-SUMMARY.md
@.planning/phases/04-documentation/04-02-SUMMARY.md
@.planning/phases/04-documentation/04-03-SUMMARY.md

Source files for verification:
@README.md (current — use as content source, but fix inaccuracies)
@Cargo.toml (version number, feature flags)
@src/main.rs (verify quickstart commands work)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Overhaul README.md (DOC-01)</name>
  <files>README.md</files>
  <action>
Rewrite README.md as a clear, structured entry point. British English throughout. The README should be shorter and more focused than the current 629-line version — detailed content now lives in docs/ files.

**CRITICAL accuracy rules from research:**
- Encryption is XChaCha20-Poly1305 via libsodium (NOT age crate)
- KDF is Argon2id (NOT scrypt)
- Use `sss keys generate` (NOT `sss keygen`)
- Use `sss seal --project` (NOT `sss process`)
- Quickstart MUST start with `sss keys generate` before `sss init`

**Structure (target ~250-350 lines):**

1. **Title and One-Line Description**
   ```
   # sss - Secret String Substitution

   Transparent encryption of secrets within files using XChaCha20-Poly1305, with multi-user key management and git integration.
   ```

2. **Features** (concise bullet list, 10-15 items max)
   - Draw from existing README Features section but keep brief
   - Mention: marker-based encryption, multi-user, git hooks, FUSE, 9P, deterministic nonces, Argon2id KDF, system keyring, Emacs integration
   - Each feature: one line, no sub-bullets

3. **Installation**
   - **cargo install** (primary path — check if `cargo install sss` works from crates.io, or if it is build-from-source only)
   - **Build from source**: git clone + cargo build --release
   - **Optional features**: FUSE (`--features fuse`), 9P (`--features ninep`) with platform-specific libfuse3 install commands
   - **Pre-built packages**: brief mention of DEB, RPM, Alpine with links to build scripts
   - Link to docs/INSTALLATION.md for detailed instructions

4. **Quick Start** (5-step numbered sequence with working commands)
   ```
   1. Generate a keypair: sss keys generate
   2. Initialise a project: sss init alice
   3. Mark secrets: echo "password=⊕{my-secret}" > config.txt
   4. Seal the file: sss seal -x config.txt
   5. Open the file: sss open config.txt
   ```
   Each step: one command + one-line explanation. No optional flags, no alternatives — the simplest working path.

5. **String Patterns** (brief — 4 lines)
   - `⊕{content}` / `o+{content}` — plaintext markers
   - `⊠{content}` — ciphertext marker (after sealing)
   - Link to docs/marker-format.md for full reference

6. **Core Commands** (condensed table or grouped list)
   - Keep the essential commands from the current README but remove the exhaustive listing
   - Group: File Operations (seal, open, render, edit), Project (init, status, users), Keys (generate, list, pubkey, rotate), Settings, Git Hooks
   - Link to docs/usage-guide.md for complete workflow documentation

7. **Multi-User Collaboration** (brief — 5-6 lines)
   - Key exchange workflow in 3 steps
   - Link to docs/usage-guide.md#team-collaboration

8. **Security** (brief summary — 5-6 lines)
   - Algorithm list: XChaCha20-Poly1305, Argon2id, X25519, Ed25519, BLAKE2b
   - What's protected: secret content within markers
   - Link to docs/security-model.md for full security model

9. **Emacs Integration** (brief — 4-5 lines)
   - sss-mode provides transparent decrypt-on-open and re-seal-on-save
   - Link to docs/sss-mode-guide.md for installation and usage

10. **Optional Features** (brief)
    - FUSE filesystem: transparent rendering mount
    - 9P server: network-transparent access
    - Agent daemon: key caching (experimental)

11. **Building and Development** (condensed)
    - Requirements: Rust 2024 edition (1.85+), libsodium
    - cargo test, cargo clippy
    - Binaries: sss, sss-agent, sss-askpass-tty, sss-askpass-gui

12. **Documentation** (link table)
    - docs/usage-guide.md — Common workflows
    - docs/architecture.md — Technical architecture
    - docs/security-model.md — Security model and algorithms
    - docs/marker-format.md — Marker format reference
    - docs/configuration.md — Configuration reference
    - docs/sss-mode-guide.md — Emacs integration guide
    - Also link existing docs that are still relevant: ARCHITECTURE.md, SECURITY.md, CONTRIBUTING.md, CHANGELOG.md, LICENCE, docs/CRYPTOGRAPHY.md, docs/KEY_MANAGEMENT.md, docs/SECRETS_FILE_FORMAT.md, docs/INSTALLATION.md

13. **Licence** — ISC (one line + link)

14. **Acknowledgements** — Keep existing (libsodium, fuser, rust-9p, similar, aho-corasick)

**What to REMOVE from current README:**
- The exhaustive command reference (moved to docs/usage-guide.md and docs/configuration.md)
- The full Code Structure listing (moved to docs/architecture.md)
- Detailed configuration examples (moved to docs/configuration.md)
- Detailed security feature listing (moved to docs/security-model.md)

**What to PRESERVE from current README:**
- General structure flow (features → install → quickstart → commands → docs → licence)
- Acknowledgements section
- Licence section
- The correct factual content (after fixing inaccuracies)
  </action>
  <verify>
1. File exists at README.md
2. `grep -c "age" README.md` — should be 0 (no age crate claims) or only in acknowledgements context
3. `grep -c "scrypt" README.md` — should be 0
4. `grep "sss keys generate" README.md` — should appear in quickstart
5. `grep "docs/usage-guide.md" README.md` — should appear (cross-link)
6. `grep "docs/security-model.md" README.md` — should appear (cross-link)
7. `grep "docs/architecture.md" README.md` — should appear (cross-link)
8. `grep "docs/marker-format.md" README.md` — should appear (cross-link)
9. `grep "docs/configuration.md" README.md` — should appear (cross-link)
10. `grep "docs/sss-mode-guide.md" README.md` — should appear (cross-link)
11. `wc -l README.md` — should be roughly 250-350 lines (not 629)
12. British English: `grep -ci "initialise\|organisation\|colour\|behaviour" README.md` — at least 1
  </verify>
  <done>
README.md is overhauled with a clear quickstart (starting from key generation), accurate installation instructions, correct cryptographic claims, and links to all six docs/ files. Shorter and more focused than the previous version. No age/scrypt inaccuracies.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify all cross-links resolve</name>
  <files>README.md</files>
  <action>
After Task 1 completes, verify that every documentation link in README.md points to a file that actually exists. Run a check for each linked path.

Also verify that the docs/ files created in Plans 01-03 exist:
- docs/usage-guide.md
- docs/security-model.md
- docs/marker-format.md
- docs/architecture.md
- docs/configuration.md
- docs/sss-mode-guide.md

And that existing docs still referenced in README.md exist:
- ARCHITECTURE.md
- SECURITY.md
- CONTRIBUTING.md
- CHANGELOG.md
- LICENCE
- docs/CRYPTOGRAPHY.md
- docs/KEY_MANAGEMENT.md
- docs/SECRETS_FILE_FORMAT.md
- docs/INSTALLATION.md

If any link is broken, fix it in README.md. If a referenced file does not exist, either remove the link or add a note that it is pending.
  </action>
  <verify>
All documentation links in README.md resolve to existing files. Run: `for f in $(grep -oP '\[.*?\]\(((?!http)[^)]+)\)' README.md | grep -oP '\(([^)]+)\)' | tr -d '()'); do test -f "$f" && echo "OK: $f" || echo "MISSING: $f"; done` — all should print "OK".
  </verify>
  <done>
Every documentation link in README.md points to an existing file. No broken links.
  </done>
</task>

</tasks>

<verification>
1. README.md exists and is shorter than the original (under 400 lines)
2. Quickstart starts with `sss keys generate` (step 1)
3. No age crate or scrypt claims anywhere in README.md
4. All six docs/ files from Plans 01-03 are linked
5. All existing referenced docs (ARCHITECTURE.md, SECURITY.md, etc.) are still linked
6. All links resolve to existing files
7. British English throughout
</verification>

<success_criteria>
- README.md provides a clear path from discovery to working usage
- Quickstart is a 5-step sequence with correct, working commands
- Installation covers cargo, build from source, and pre-built packages
- All detailed content is delegated to docs/ files via links
- No propagation of inaccurate cryptographic claims
- British English throughout
</success_criteria>

<output>
After completion, create `.planning/phases/04-documentation/04-04-SUMMARY.md`
</output>
