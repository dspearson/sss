---
phase: 01-cli-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-cli-foundation/01-01-SUMMARY.md
autonomous: true
requirements:
  - CLI-01
  - CLI-02

must_haves:
  truths:
    - "cargo test --test e2e_cli_workflows passes with all existing tests green"
    - "The existing tests already cover: render stdout output, seal -x stderr confirmation, open stdout output, and non-interactive env var"
    - "The audit confirms: stdout is clean (no contamination) for both render and open; seal --in-place writes nothing to stdout"
    - "The marker byte sequence ⊠{ (U+22A0 + 0x7b) is confirmed from src/constants.rs and matches has_sss_markers usage"
  artifacts:
    - path: ".planning/phases/01-cli-foundation/01-01-SUMMARY.md"
      provides: "Audit results documenting which success criteria are already satisfied and which gaps remain"
  key_links:
    - from: "tests/e2e_cli_workflows.rs"
      to: "src/commands/process.rs"
      via: "binary invocation via env!(CARGO_BIN_EXE_sss)"
      pattern: "CARGO_BIN_EXE_sss"
    - from: "src/constants.rs"
      to: "src/commands/process.rs"
      via: "has_sss_markers uses literal ⊠{ string"
      pattern: "MARKER_CIPHERTEXT"
---

<objective>
Audit the existing sss CLI implementation and test suite against the Phase 1 roadmap success criteria. Run the existing e2e test suite to confirm it passes. Document which criteria are already satisfied by existing tests and which gaps remain for Plan 02 to address.

Purpose: Establish a verified baseline before adding new tests. The research confirmed both CLI-01 and CLI-02 are implemented; this plan confirms the tests already in the repo pass and maps them to the roadmap success criteria.
Output: Passing test run + SUMMARY.md documenting the audit findings.
</objective>

<execution_context>
@/home/dsp/.claude/get-shit-done/workflows/execute-plan.md
@/home/dsp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cli-foundation/1-RESEARCH.md
@src/constants.rs
@src/commands/process.rs
@tests/e2e_cli_workflows.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run existing e2e test suite and confirm it passes</name>
  <files>tests/e2e_cli_workflows.rs</files>
  <action>
Run the existing e2e_cli_workflows test suite against the compiled sss binary and capture results:

```
cargo test --test e2e_cli_workflows 2>&1 | tee /tmp/e2e_results.txt
```

If any tests fail, inspect the failure output and note the failing test name and error. Do NOT fix failures in this task — record them for the SUMMARY.

After the run, verify each of the following Phase 1 success criteria is covered by at least one passing test:

1. `sss render FILE` writes decrypted plaintext to stdout — covered by `e2e_workflow_render_to_stdout` and `e2e_workflow_seal_then_render_recovers_plaintext`
2. `sss render FILE` exits 0 on success — covered by `run_ok` assertions in all render tests
3. `sss render FILE` exits non-zero with stderr message on auth/config failure — covered by `run_fail` tests for missing file; CHECK whether there is a test that exercises keystore auth failure specifically (no matching keypair for a project)
4. `sss seal --in-place FILE` re-encrypts in place — covered by `e2e_workflow_seal_in_place_message` and `e2e_workflow_full_roundtrip_seal_open_render`
5. `sss seal --in-place FILE` exits 0 on success — covered by `run_ok` assertions in seal -x tests
6. `sss seal --in-place FILE` keeps stdout EMPTY (confirmation on stderr only) — CHECK: the `e2e_workflow_seal_in_place_message` test captures (_, stderr) but does NOT assert stdout == "". Look for whether stdout emptiness is explicitly asserted anywhere.
7. `--non-interactive` flag prevents TTY blocking — the test harness uses `SSS_NONINTERACTIVE=1` env var (which is equivalent to the flag). CHECK: whether a test explicitly uses the `--non-interactive` CLI flag rather than the env var.

Record the exact test names covering each criterion, and record which criteria have NO covering test.
  </action>
  <verify>
`cargo test --test e2e_cli_workflows` exits 0 (all tests pass). Record output for SUMMARY.
  </verify>
  <done>All existing e2e_cli_workflows tests pass. Each of the 7 criteria above has been checked — covered tests named, uncovered criteria listed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Confirm marker constants and write audit SUMMARY</name>
  <files>.planning/phases/01-cli-foundation/01-01-SUMMARY.md</files>
  <action>
Read `src/constants.rs` and `src/commands/process.rs` to confirm:
- `MARKER_CIPHERTEXT` is `"⊠"` (U+22A0, UTF-8: `\xe2\x8a\xa0`)
- `has_sss_markers` uses the literal `"⊠{"` (not MARKER_CIPHERTEXT constant — check both)
- The sealed-file detection byte sequence is: `\xe2\x8a\xa0\x7b` (4 bytes)

Then write `.planning/phases/01-cli-foundation/01-01-SUMMARY.md` using the summary template. The SUMMARY must include:

**Confirmed Facts (for downstream Phase 2 Emacs mode development):**
- Exact invocation for decrypted plaintext: `sss --non-interactive render /path/to/file.sss` → stdout only, stderr empty on success
- Exact invocation for re-sealing: `sss --non-interactive seal --in-place /path/to/file.sss` → stdout empty, stderr has confirmation, file rewritten
- Exact invocation for open (markers visible): `sss --non-interactive open /path/to/file.sss` → stdout has `⊕{...}` markers
- Exit codes: 0 on success, 1 on any failure (anyhow propagation, no custom exit calls)
- Sealed marker: `⊠{` — U+22A0 + 0x7b — UTF-8 bytes `\xe2\x8a\xa0\x7b`
- Non-interactive: use env var `SSS_NONINTERACTIVE=1` OR CLI flag `--non-interactive` (both set the env var internally)
- Stdout contamination: NONE — all informational messages use `eprintln!`; `print!` is only for content output

**Test Coverage Gaps (for Plan 02):**
List which of the 7 criteria from Task 1 have no explicit test covering them. These are the inputs to Plan 02.

**What Phase 1 does NOT require:**
- No new Rust source code changes
- No new CLI flags
- No changes to src/commands/process.rs or src/main.rs
  </action>
  <verify>
`.planning/phases/01-cli-foundation/01-01-SUMMARY.md` exists, contains a section on confirmed facts, a section on test coverage gaps, and references the exact source locations verified.
  </verify>
  <done>SUMMARY.md exists with: passing test count, confirmed invocation patterns for Emacs, confirmed marker bytes, and explicit list of gaps that Plan 02 will fill.
  </done>
</task>

</tasks>

<verification>
1. `cargo test --test e2e_cli_workflows` exits 0
2. `01-01-SUMMARY.md` exists under `.planning/phases/01-cli-foundation/`
3. SUMMARY contains the 3 required Emacs invocation patterns (render, seal --in-place, open)
4. SUMMARY contains the confirmed marker byte sequence `\xe2\x8a\xa0\x7b`
5. SUMMARY lists any test coverage gaps found (inputs to Plan 02)
</verification>

<success_criteria>
- All pre-existing e2e_cli_workflows tests pass (no regressions)
- The three roadmap success criteria for Phase 1 are mapped to specific existing tests (or flagged as gaps)
- Marker byte sequence confirmed from source and documented
- The confirmed CLI interface is recorded in a form that Phase 2 (Emacs mode) can reference directly
</success_criteria>

<output>
After completion, create `.planning/phases/01-cli-foundation/01-01-SUMMARY.md` using the summary template with the audit findings documented above.
</output>
