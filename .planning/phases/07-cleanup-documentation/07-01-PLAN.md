---
phase: 07-cleanup-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/emacs/ (removed)
  - README.md
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-03

must_haves:
  truths:
    - "The plugins/emacs/ directory does not exist in the repo"
    - "No file in the codebase references plugins/emacs/ (excluding .planning/ history)"
    - "The README.md Emacs section describes the consolidated emacs/sss-mode.el package and its v1.1 capabilities"
  artifacts:
    - path: "README.md"
      provides: "Updated Emacs Integration section"
      contains: "emacs/sss-mode.el"
  key_links:
    - from: "README.md"
      to: "docs/sss-mode-guide.md"
      via: "documentation link"
      pattern: "docs/sss-mode-guide\\.md"
---

<objective>
Remove the legacy `plugins/emacs/` directory and update the README.md Emacs section to reflect the consolidated single-file package.

Purpose: After v1.1 consolidation (Phases 5-6), all features from `plugins/emacs/` have been ported into `emacs/sss-mode.el`. The old directory must be removed to avoid confusion, and the README must describe what users actually get.

Output: `plugins/emacs/` deleted from git, README.md Emacs section rewritten.
</objective>

<execution_context>
@/home/dsp/.claude/get-shit-done/workflows/execute-plan.md
@/home/dsp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@emacs/sss-mode.el
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove plugins/emacs/ directory and verify no remaining references</name>
  <files>plugins/emacs/ (removed)</files>
  <action>
Run `git rm -r plugins/emacs/` to remove the entire legacy directory from version control.

After removal, grep the codebase for remaining references to `plugins/emacs`:
```bash
grep -r "plugins/emacs" --include="*.md" --include="*.el" --include="*.toml" --include="*.rs" --include="*.sh" .
```

Exclude `.planning/` files from the check -- those are historical planning artifacts that naturally reference the old directory in context. The requirement is that no USER-FACING file (README, docs/, src/, emacs/, config files) references the old path.

If any user-facing references are found, update them to point to `emacs/sss-mode.el` instead.
  </action>
  <verify>
```bash
# Directory must not exist
test ! -d plugins/emacs/ && echo "PASS: directory removed"

# No user-facing references (exclude .planning/)
grep -r "plugins/emacs" --include="*.md" --include="*.el" --include="*.toml" --include="*.rs" --include="*.sh" . | grep -v "^\.planning/" | grep -v "^\./.planning/" && echo "FAIL: references remain" || echo "PASS: no user-facing references"
```
  </verify>
  <done>The `plugins/emacs/` directory does not exist in the working tree or git index, and no user-facing file references it.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite README.md Emacs Integration section</name>
  <files>README.md</files>
  <action>
Replace the current `## Emacs Integration` section in README.md (lines ~190-199) with a rewritten section that covers:

1. **Package location**: Single file at `emacs/sss-mode.el` (v1.1)
2. **Core capability**: Transparent decrypt-on-open and re-seal-on-save (unchanged from v1.0)
3. **New v1.1 features** (brief bullet list):
   - Region encrypt/decrypt (`C-c C-e` / `C-c C-d`)
   - Toggle marker at point (`C-c C-t`)
   - Preview decrypted secret at point (`C-c C-v`)
   - Overlay mode for visual marker highlighting
   - Auth-source integration for passphrase caching
   - Transient command menu (`C-c C-m`)
4. **Evil integration** (when evil is loaded): `ge`/`gd`/`gt` operators, `is`/`as` text objects
5. **Doom Emacs**: `SPC e` leader prefix, `, e` localleader prefix
6. **Installation snippet**: `(add-to-list 'load-path "/path/to/sss/emacs/")` + `(require 'sss-mode)`
7. **Doom setup note**: Copy to `~/.config/doom/lisp/`, add `(load! "lisp/sss-mode")` in config.el
8. **Link** to full guide: `docs/sss-mode-guide.md`

Keep the section concise -- the full guide has details. Do NOT change any other section of the README.
  </action>
  <verify>
Read the updated README.md and confirm:
- The Emacs section mentions `emacs/sss-mode.el` (not `plugins/emacs/`)
- It lists v1.1 features (region ops, toggle, preview, overlay, auth-source, transient, evil, doom)
- It links to `docs/sss-mode-guide.md`
- No other sections are modified
  </verify>
  <done>README.md Emacs Integration section describes the consolidated v1.1 package with installation, key features, Doom setup, and a link to the full guide.</done>
</task>

</tasks>

<verification>
1. `ls plugins/emacs/` returns error (directory does not exist)
2. `grep -r "plugins/emacs" . | grep -v ".planning/"` returns no matches
3. README.md Emacs section references `emacs/sss-mode.el` and lists v1.1 features
4. README.md links to `docs/sss-mode-guide.md`
</verification>

<success_criteria>
- `plugins/emacs/` directory is gone from the repo
- No user-facing file references the old directory
- README.md Emacs section accurately describes the consolidated package
</success_criteria>

<output>
After completion, create `.planning/phases/07-cleanup-documentation/07-01-SUMMARY.md`
</output>
